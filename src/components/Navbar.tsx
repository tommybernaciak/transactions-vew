import { FC } from 'react';

const Navbar: FC = () => {
  return (
    <div className="w-full px-2 py-2 lg:py-4 bg-stone-500">
      <h1 className="text-3xl font-bold">Transactions Panel</h1>
    </div>
  );
};

export default Navbar;
